# 2026-03-23 TODO

## Deferred Option B: Async Persistence Command Queue

当前已确认的主方案是：

- persisted catalog domain 与 UI state 分层
- logging-style root abstraction
- `redb` 作为首选本地持久化介质
- `AssetCatalogRepository` 作为 store abstraction

以下方案暂不作为当前确认基线，但保留为后续演进项：

### Option B

使用 `Tokio task / command queue` 做异步持久化写入，要求：

- UI 与 store 解耦，mutation 不直接在 UI 回调里写磁盘
- create / rename / delete / edit SSH connection 通过 command queue 排队
- 明确 ack 语义
- 明确失败回滚语义
- 明确窗口关闭时的 flush 策略
- 明确崩溃前未落盘命令的恢复语义
- 明确导入导出与未来远程同步如何复用同一 command pipeline

### Why deferred

- 当前阶段先把 persisted domain、root resolver、`redb` store 和 README 目录说明边界落稳；
- 若过早引入 async queue，会同时扩大以下状态面：
  - in-memory optimistic state
  - durable state
  - queue state
  - shutdown flush state
  - retry / rollback state
- 更适合作为“持久化基线稳定后”的第二阶段演进。

### Follow-up notes

- README 需要补充资产数据目录解析说明；
- 若未来启用该方案，需要单独产出一份 command queue design / implementation plan；
- 不能让 UI 直接依赖 queue 内部状态结构。
