// Central semantic theme tokens shared across the Slint desktop shell.

export global ThemeTokens {
    in property <bool> dark-mode: true;

    out property <brush> titlebar-background: dark-mode ? #10151d : #eef2f5;
    out property <brush> tabbar-background: dark-mode ? #10151d : #eef2f5;
    out property <brush> sidebar-background: dark-mode ? #10151d : #eef2f5;
    out property <brush> sidebar-panel-background: dark-mode ? #111821 : #f0f3f6;
    out property <brush> right-panel-background: dark-mode ? #111821 : #f0f3f6;
    out property <brush> terminal-frame-background: dark-mode ? #141b24 : #fafafa;
    out property <brush> separator: dark-mode ? #1b2530 : #d8dee6;
    out property <brush> hairline: dark-mode ? #ffffff22 : #1c273324;

    out property <brush> accent: dark-mode ? #e6b450 : #ffaa33;
    out property <brush> link-accent: dark-mode ? #89a9cf : #5077a7;
    out property <brush> focus-ring: dark-mode ? #7d9bc2 : #7e9ec5;

    out property <brush> text-primary: dark-mode ? #c5c1b8 : #5c6166;
    out property <brush> text-secondary: dark-mode ? #9aa4ae : #7a838c;
    out property <brush> text-muted: dark-mode ? #7d8790 : #8a939c;
    out property <brush> text-inactive: dark-mode ? #768296 : #7b8a9c;

    out property <brush> window-surface: dark-mode ? #141b23 : #f2f5f8;
    out property <brush> titlebar-surface: titlebar-background;
    out property <brush> activity-surface: sidebar-background;
    out property <brush> assets-surface: sidebar-panel-background;
    out property <brush> tabbar-surface: tabbar-background;
    out property <brush> tab-active-surface: dark-mode ? #223040 : #ffffff;
    out property <brush> tab-inactive-surface: tabbar-background;
    out property <brush> tab-hover-surface: dark-mode ? #202b38 : #e8edf4;
    out property <brush> tab-active-line: dark-mode ? #7a97bc : #6388b4;
    out property <brush> tab-active-indicator: tab-active-line;
    out property <brush> workspace-surface: terminal-frame-background;
    out property <brush> panel-surface: right-panel-background;
    out property <brush> inspector-surface: right-panel-background;
    out property <brush> utility-panel-surface: right-panel-background;
    out property <brush> utility-panel-header-surface: dark-mode ? #212b36 : #f7fafc;
    out property <brush> utility-panel-section-surface: dark-mode ? #151d26 : #f8fbfd;
    out property <brush> utility-panel-row-hover-surface: dark-mode ? #24303c : #ebf2f8;
    out property <brush> utility-panel-border: hairline;
    out property <brush> utility-panel-shadow-soft: dark-mode ? #00000016 : #0f172a08;
    out property <brush> utility-panel-shadow-strong: dark-mode ? #00000026 : #0f172a10;
    out property <brush> utility-panel-glow: dark-mode ? #7d9bc210 : #587daa0c;
    out property <brush> utility-panel-progress-track: dark-mode ? #ffffff10 : #1c273310;
    out property <brush> utility-panel-empty-surface: dark-mode ? #202a36 : #edf3f8;
    out property <brush> conflict-dialog-surface: dark-mode ? #1a2430 : #f6f9fc;
    out property <brush> conflict-dialog-header-surface: dark-mode ? #202a36 : #fbfcfe;
    out property <brush> conflict-dialog-section-surface: dark-mode ? #141d27 : #ffffff;
    out property <brush> conflict-dialog-path-surface: dark-mode ? #111923 : #f7faff;
    out property <brush> conflict-dialog-border: dark-mode ? #ffffff24 : #8ea4bf30;
    out property <brush> conflict-dialog-section-border: separator;
    out property <brush> conflict-dialog-shadow-soft: utility-panel-shadow-soft;
    out property <brush> conflict-dialog-shadow-strong: utility-panel-shadow-strong;
    out property <brush> conflict-dialog-glow: dark-mode ? #7d9bc210 : #587daa0b;
    out property <brush> conflict-dialog-recommended-surface: accent;
    out property <brush> conflict-dialog-recommended-hover-surface: dark-mode ? #7f9dc2 : #6a8eb7;
    out property <brush> conflict-dialog-destructive-border: dark-mode ? #cf9ca58a : #bc6f7f70;
    out property <brush> conflict-dialog-destructive-surface: dark-mode ? #251d23 : #fbf3f5;
    out property <brush> conflict-dialog-destructive-hover-surface: dark-mode ? #302329 : #f8eaee;
    out property <brush> modal-backdrop: dark-mode ? #030811ac : #0f172a18;
    out property <brush> modal-surface: dark-mode ? #1a2330 : #f9fbfe;
    out property <brush> modal-header-surface: dark-mode ? #202a37 : #fcfdff;
    out property <brush> modal-body-surface: dark-mode ? #18212c : #f8fbff;
    out property <brush> modal-footer-surface: dark-mode ? #1a2330 : #f9fbfe;
    out property <brush> modal-footer-rail-surface: dark-mode ? #243142 : #ffffff;
    out property <brush> modal-footer-rail-border: hairline;
    out property <brush> modal-section-surface: dark-mode ? #111a25 : #ffffff;
    out property <brush> modal-section-alt-surface: dark-mode ? #15202b : #f5f8fc;
    out property <brush> modal-border: hairline;
    out property <brush> modal-inner-border: separator;
    out property <brush> modal-shadow-soft: utility-panel-shadow-soft;
    out property <brush> modal-shadow-strong: utility-panel-shadow-strong;
    out property <brush> modal-glow: dark-mode ? #7d9bc210 : #587daa0c;
    out property <brush> modal-section-shadow: dark-mode ? #0000000c : #0f172a06;
    out property <brush> modal-field-surface: dark-mode ? #101923 : #ffffff;
    out property <brush> modal-field-border: dark-mode ? #ffffff16 : #8ea4bf24;
    out property <brush> modal-field-hover-border: dark-mode ? #ffffff22 : #8ea4bf34;
    out property <brush> modal-button-primary-surface: accent;
    out property <brush> modal-button-primary-hover-surface: dark-mode ? #7d9bc2 : #6a8eb7;
    out property <brush> modal-button-primary-pressed-surface: dark-mode ? #5f7ea7 : #4e729c;
    out property <brush> modal-button-secondary-surface: dark-mode ? #1d2733 : #e8f0fa;
    out property <brush> modal-button-secondary-hover-surface: dark-mode ? #24303f : #dee8f5;
    out property <brush> modal-button-secondary-pressed-surface: dark-mode ? #2a3646 : #d5e1f1;
    out property <brush> modal-button-danger-surface: conflict-dialog-destructive-surface;
    out property <brush> modal-button-danger-hover-surface: conflict-dialog-destructive-hover-surface;
    out property <brush> modal-button-danger-border: conflict-dialog-destructive-border;

    out property <length> modal-radius: 16px;
    out property <length> modal-section-radius: 12px;
    out property <length> modal-control-radius: 10px;
    out property <length> modal-header-height: 58px;
    out property <length> modal-footer-min-height: 72px;

    out property <brush> sidebar-item-hover-background: dark-mode ? #22303d : #e4ebf3;
    out property <brush> sidebar-item-selected-background: dark-mode ? #293846 : #dce6f2;
    out property <brush> sidebar-item-selected-border: dark-mode ? #6c88ae55 : #6e8cb333;
    out property <brush> sidebar-hover-surface: sidebar-item-hover-background;
    out property <brush> sidebar-selected-surface: sidebar-item-selected-background;
    out property <brush> explorer-row-hover-surface: sidebar-item-hover-background;
    out property <brush> explorer-row-selected-surface: sidebar-item-selected-background;

    out property <brush> input-surface: dark-mode ? #161f29 : #ffffff;
    out property <brush> input-border: dark-mode ? #334252 : #d2dbe6;
    out property <brush> input-focus-ring: focus-ring;

    out property <brush> divider-subtle: separator;
    out property <brush> divider-strong: hairline;

    out property <brush> control-hover-surface: dark-mode ? #202b37 : #eaf0f7;
    out property <brush> control-active-surface: dark-mode ? #1d2732 : #f0f5fb;
    out property <brush> control-pressed-surface: dark-mode ? #263443 : #dde7f2;
    out property <brush> control-disabled-surface: dark-mode ? #171f28 : #eef3f7;
    out property <brush> menu-row-hover-surface: dark-mode ? #25313e : #edf3fb;
    out property <brush> menu-row-open-surface: dark-mode ? #2b3848 : #e3ebf7;

    out property <brush> status-busy-surface: dark-mode ? #223241 : #e8eef8;
    out property <brush> status-success-surface: dark-mode ? #20372a : #e7f4ea;
    out property <brush> status-error-surface: dark-mode ? #3b282d : #f9eaed;
    out property <brush> status-busy-accent: dark-mode ? #7b96b8 : #6b85a9;
    out property <brush> status-success-accent: dark-mode ? #7fb08d : #5e8a68;
    out property <brush> status-error-accent: dark-mode ? #c98a94 : #a55c67;
    out property <brush> status-pill-surface: dark-mode ? #1f2934 : #eef4fb;
    out property <brush> status-pill-border: dark-mode ? #314153 : #d8e2ef;
    out property <brush> danger-hover-surface: dark-mode ? #a73550 : #d84b6a;
    out property <brush> danger-pressed-surface: dark-mode ? #8f2943 : #c53d5c;
    out property <brush> danger-fg: #ffffff;

    out property <brush> ssh-status-page-surface: dark-mode ? #101923 : #f5f7fb;
    out property <brush> ssh-status-page-atmosphere: dark-mode ? #24466f0e : #d7e5ff16;
    out property <brush> ssh-status-page-atmosphere-core: dark-mode ? #31588608 : #e3eeff0c;
    out property <brush> ssh-status-card-surface: dark-mode ? #162231 : #ffffff;
    out property <brush> ssh-status-card-elevated-surface: dark-mode ? #1b2a3b : #fbfdff;
    out property <brush> ssh-status-card-border: dark-mode ? #a5b7cb38 : #d5dfea;
    out property <brush> ssh-status-inner-highlight: dark-mode ? #d7e6ff14 : #ffffff8a;
    out property <brush> ssh-status-hop-divider: dark-mode ? #9fb0c62c : #a0afc140;
    out property <brush> ssh-status-neutral-surface: dark-mode ? #192637 : #eef3f9;
    out property <brush> ssh-status-neutral-border: dark-mode ? #7f94ab54 : #c7d3e2;
    out property <brush> ssh-status-neutral-fg: dark-mode ? #d8e2ef : #46566a;
    out property <brush> ssh-status-hop-node-surface: dark-mode ? #18243378 : #f8fbff;
    out property <brush> ssh-status-hop-node-border: dark-mode ? #7f91a626 : #d7e1ec;
    out property <brush> ssh-status-hop-current-surface: dark-mode ? #1c2b3c : #f4f8fe;
    out property <brush> ssh-status-hop-current-border: dark-mode ? #8aa0bf72 : #9eb4cc;
    out property <brush> ssh-status-hop-icon-surface: dark-mode ? #182435 : #f8fbff;
    out property <brush> ssh-status-hop-icon-border: dark-mode ? #8ea1b618 : #d9e3ee;
    out property <brush> ssh-status-hop-current-icon-surface: dark-mode ? #21344a : #eef5ff;
    out property <brush> ssh-status-hop-current-icon-border: dark-mode ? #92a9cb5a : #9eb4cc;
    out property <brush> ssh-status-hop-icon-glow: dark-mode ? #8faad10e : #6f8eb70c;
    out property <brush> ssh-status-progress-accent: dark-mode ? #8ea7c6 : #6f8eb2;
    out property <brush> ssh-status-hero-surface: dark-mode ? #1d2231 : #f6f7fb;
    out property <brush> ssh-status-hero-border: dark-mode ? #9db2d856 : #afbed66b;
    out property <brush> ssh-status-hero-icon: dark-mode ? #edf3ff : #5b6786;
    out property <brush> ssh-status-hero-glow: dark-mode ? #90a9d610 : #a9b8d408;
    out property <brush> ssh-status-field-surface: dark-mode ? #172637 : #f8fbff;
    out property <brush> ssh-status-field-border: dark-mode ? #90a7c224 : #d7e1ec;
    out property <brush> ssh-status-footer-surface: dark-mode ? #18253624 : #f9fbfe26;
    out property <brush> ssh-status-amber-surface: dark-mode ? #2b2418 : #fff8e8;
    out property <brush> ssh-status-amber-border: dark-mode ? #85673b : #e4bc72;
    out property <brush> ssh-status-amber-fg: dark-mode ? #efbc5a : #8b6519;
    out property <brush> ssh-status-green-surface: dark-mode ? #4ab66f : #4ab66f;
    out property <brush> ssh-status-green-hover-surface: dark-mode ? #57c67b : #57c67b;
    out property <brush> ssh-status-green-pressed-surface: dark-mode ? #3b9159 : #3b9159;
    out property <brush> ssh-status-warning-surface: dark-mode ? #32271f : #fff6ea;
    out property <brush> ssh-status-warning-border: dark-mode ? #896541 : #e8bc7e;
    out property <brush> ssh-status-warning-fg: dark-mode ? #f0c27a : #8b5d1f;
    out property <brush> ssh-status-danger-border: dark-mode ? #d4707d : #c2606e;
    out property <brush> ssh-status-danger-surface: dark-mode ? #2c2027 : #fff2f4;

    out property <length> ssh-status-card-radius: 18px;
    out property <length> ssh-status-panel-radius: 14px;
    out property <length> ssh-status-pill-radius: 17px;
    out property <length> ssh-status-field-radius: 12px;
    out property <length> ssh-status-hop-radius: 15px;

    out property <brush> terminal-canvas-surface: dark-mode ? #0a0e14 : #f8f9fa;
    out property <brush> terminal-default-fg: dark-mode ? #c5c1b8 : #5c6166;
    out property <brush> terminal-soft-fg: dark-mode ? #99a1a8 : #6c7680;
    out property <brush> terminal-dim-fg: dark-mode ? #5c6773 : #828c99;
    out property <brush> terminal-default-bg: terminal-canvas-surface;
    out property <brush> terminal-cursor-fg: dark-mode ? #0a0e14 : #f8f9fa;
    out property <brush> terminal-cursor-bg: dark-mode ? #e6b450 : #ffaa33;
    out property <brush> terminal-selection-surface: dark-mode ? #2a3541c7 : #55b4d433;
    out property <brush> terminal-scrollbar-track-surface: dark-mode ? #111821 : #f0f3f6;
    out property <brush> terminal-scrollbar-thumb-surface: dark-mode ? #2f3944 : #d1d7de;
    out property <brush> terminal-scrollbar-thumb-active-surface: dark-mode ? #3c4856 : #c1c8d1;
}
