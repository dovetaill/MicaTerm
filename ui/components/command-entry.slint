// Styled command entry surface reserved for terminal and quick-action input affordances.

import { ThemeTokens } from "../theme/tokens.slint";

export component CommandEntry inherits Rectangle {
    border-radius: 10px;
    border-width: 1px;
    border-color: ThemeTokens.input-border;
    background: ThemeTokens.input-surface;
}
