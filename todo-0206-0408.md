# Terminal Memory Backend Purge TODO

日期: 2026-04-09
执行者: Codex

## 当前阶段明确不直接实现

- `active-window idle cache shrink` 先只做应用层 transient cache shrink
- 不在当前改动里直接接入更深层 Slint / Skia / DXGI purge
- 不在可见终端 surface 仍活跃时主动释放 renderer host 或 native surface

## 后续待办

- 接入 `SkGraphics::PurgeAllCaches()`
  - 目标: 主动回收 Skia 全局图形缓存
  - 触发时机: 窗口最小化、长时间 idle、surface 消失后的延迟清理点
  - 风险: 需要验证 purge 后首帧重建成本, 避免切回窗口时明显卡顿

- 接入 `GrDirectContext::performDeferredCleanup()`
  - 目标: 清理 GPU 资源与延迟释放对象, 不只清理上层 presenter cache
  - 建议: 优先在 backend/context 仍然有效且处于 idle 的安全点触发
  - 风险: 需要确认和 Slint / Skia renderer 生命周期对齐, 避免 context 状态错乱

- 接入 `IDXGIDevice3::Trim()`
  - 目标: 在 Windows/D3D 路径主动提示驱动回收不再活跃的显存/资源驻留
  - 触发时机: 窗口 hidden/minimized/occluded, 或 workspace terminal 长时间 idle
  - 风险: 需要保证调用点只在 DXGI device 可用时执行, 并评估恢复渲染的抖动

- 研究并补齐 Slint/Skia backend 的 cache purge 对称路径
  - 重点: `i-slint-renderer-skia` 里的 `layer_cache` / surface cache 生命周期
  - 目标: 让 terminal surface 清空后, 底层 retained cache 也有对称释放点
  - 风险: 需要先确认哪些缓存是应用可控、哪些属于 backend 全局状态

- 增加 hide/minimize/occluded -> `suspend()` / `resume()` 生命周期路径
  - 目标: 不只在 terminal surface 消失时 shrink, 也在窗口不可见时整体降驻留
  - 建议: 与平台 window visibility / occlusion 状态统一建模
  - 风险: 需要验证恢复后的字体、surface、selection overlay、native presenter 状态完整性

## 建议的验证顺序

1. 先做 backend purge 的最小可观测埋点, 只记录触发点与前后 cache stats
2. 再单独接 `SkGraphics::PurgeAllCaches()` / `performDeferredCleanup()`
3. 然后再补 `IDXGIDevice3::Trim()`
4. 最后才做 window minimize/occluded 生命周期整合

## 验收重点

- 关注 `working set` 之外, 也要看真实常驻与 commit/private usage
- 验证 `cat` 大文本、滚动、关闭标签、全部关闭窗口后的回落曲线
- 验证 purge 后重新激活窗口时没有明显首帧卡顿、黑屏或字体闪烁
- 验证多标签切换时不会因为过度 purge 导致频繁重建抖动
